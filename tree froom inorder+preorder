/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    TreeNode* builderbob(vector<int>& preorder,int prestart, int preend,vector<int>& inorder,int instart,int inend,map<int,int> &mp){
        if(prestart>preend || instart>inend) return NULL;
        TreeNode* root=new TreeNode(preorder[prestart]);
        int onward=mp[root->val];
        int numsleft=onward-instart;
        root->left=builderbob(preorder,prestart+1,prestart+numsleft,inorder,instart,onward-1,mp);
        root->right=builderbob(preorder,prestart+numsleft+1,preend,inorder,onward+1,inend,mp);
        return root;
    }
    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {
        map<int,int> mp;
        for(int i=0;i<inorder.size();i++){
            mp[inorder[i]]=i;
        }
        TreeNode* root=builderbob(preorder,0,preorder.size()-1,inorder,0,inorder.size()-1,mp);
        return root;
    }
};

// class Solution {
// public:
//     TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {
//         //if((preorder.empty() && inorder.empty())) return root;
//         if((preorder.empty() || inorder.empty())) return  NULL;
//         TreeNode* root= new TreeNode(preorder[0]);
//         int n=inorder.size();
//         int index=0;
//         for(int i=0;i<n;i++){
//             if(inorder[i]==root->val){
//                 index=i;
//             }
//         }
//         vector<int> newinleft(inorder.begin(),inorder.begin()+index+1);
//         vector<int> newinright(inorder.begin()+index+1,inorder.end());
//         vector<int> newpreleft(preorder.begin()+1,preorder.begin()+1+index);
//         vector<int> newpreright(preorder.begin()+index+1,preorder.end());
//         root->left=buildTree(newpreleft,newinleft);
//         root->right=buildTree(newpreright,newinright);
//         return root;
//     }
// };
